meta {
  name: Remove Story From Folder
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/folders/{{folderId}}/stories/{{storyId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
