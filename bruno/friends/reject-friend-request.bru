meta {
  name: Reject Friend Request (Bob rejects)
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/friends/requests/{{friendRequestId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenBob}}
}
